// util sederhana & penanda navbar aktif (kalau nanti mau dipakai)
(function(){
  // contoh: bisa dipakai untuk highlight link aktif berdasarkan pathname
  const path = location.pathname.split('/').pop();
  // (opsional) implementasi highlight kalau nav berupa <a> dengan data-page
  // untuk sekarang tidak wajib, karena kita pakai tombol <a> biasa.
})();